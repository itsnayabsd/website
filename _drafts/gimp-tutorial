Blog post banner
----------------
Use layer window to select layers in the image. Once template is completed, save it as .xcf file so that future posts can use the same template for banners. There is layers -> Scale layer option which is quite useful for scaling the layer pictures.